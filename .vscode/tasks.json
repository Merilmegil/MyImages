{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Commit and Push Images",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "set -e; git add .; git commit -m \"Update images\"; git push"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
